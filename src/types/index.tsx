// navbar list types 
export interface NavbarType {
    id: number;
    title: string;
    link: string;
}

// skills types
export interface SkillType {
    id: number;
    category: string;
    skill: string;
}

// projects types
export interface ProjectType {
    id: number;
    title: string;
    image: string;
    link: string;
}